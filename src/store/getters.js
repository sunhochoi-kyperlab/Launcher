export const isLoading = state => {
  return state.isLoading
}
export const appList = state => {
  return state.appList
}
export const currAppSlideIndex = state => {
  return state.currAppSlideIndex
}
